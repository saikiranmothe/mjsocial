<%= form_for(@serach_word) do |f| %>
  <% if @serach_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@serach_word.errors.count, "error") %> prohibited this serach_word from being saved:</h2>

      <ul>
      <% @serach_word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
